{
  "name": "code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "team-decide": "cd team-decide && npm run babel && npm run build && npm run start",
    "team-search": "cd team-search && npm run babel && npm run build && npm run start",
    "start": "concurrently --names 'nginx,page,decide,search' \"nginx -c \\\"`pwd`/webserver/nginx.conf\\\"\" \"mfserve --listen 3001 team-page\" \"npm run team-decide\" \"npm run team-search\" \"npm run open -- http://localhost:3000\"",
    "open": "sleepms 5000 && opener"
  },
  "author": "silverbirder",
  "license": "MIT",
  "dependencies": {
    "@microfrontends/serve": "^11.2.0",
    "concurrently": "^4.1.0",
    "opener": "^1.5.1",
    "sleep-ms": "^2.0.1"
  }
}